# dependencies
/node_modules

# next.js
/.next
/out

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env
.env
.env*.local

# typescript
*.tsbuildinfo
next-env.d.ts

# eval: live-site captures move over time and pull the network into CI, so keep
# them local. Committed fixture captures are force-added past this rule.
/eval/corpus/*/capture.json
!/eval/corpus/clean-light/capture.json
!/eval/corpus/dark-mode/capture.json

temp.txt
.agents/temp/